apiVersion: cloudify.windriver.com/v1
kind: CloudifyOperation
metadata:
  name: app2-dev-install
spec:
  deployment_ref: deployments/app2-dev.yaml
  workflow: install
  wait: true
  ensure_environment: true
  timeout_sec: 3600
  parameters: {}
