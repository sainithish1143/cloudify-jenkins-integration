apiVersion: cloudify.windriver.com/v1
kind: CloudifyOperation
metadata:
  name: conductor-wrcp-tester-plugin-install
spec:
  deployment_ref: deployments/conductor-wrcp-tester-plugin.yaml
  workflow: install
